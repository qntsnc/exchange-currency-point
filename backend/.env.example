# Для локального запуска вне Docker можно использовать этот файл (скопировать в .env)
# DATABASE_URL="postgres://exchange_user:exchange_password@localhost:5432/exchange_db?sslmode=disable"
APP_PORT="8080"